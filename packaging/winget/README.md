# WinGet later

After the first public GitHub Release is stable, generate a WinGet manifest with `wingetcreate new <installer-url>` and submit it to `microsoft/winget-pkgs`. The release workflow keeps stable versioned installer URLs and SHA-256 checksums specifically to make this straightforward.
